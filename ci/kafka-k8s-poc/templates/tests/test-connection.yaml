apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafka-k8s-poc.fullname" . }}-test-connection"
  labels:
    {{- include "kafka-k8s-poc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kafka-k8s-poc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
